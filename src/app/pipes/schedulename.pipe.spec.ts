import { SchedulenamePipe } from './schedulename.pipe';

describe('SchedulenamePipe', () => {
  it('create an instance', () => {
    const pipe = new SchedulenamePipe();
    expect(pipe).toBeTruthy();
  });
});
