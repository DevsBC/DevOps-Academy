import { ScheduleperiodPipe } from './scheduleperiod.pipe';

describe('ScheduleperiodPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleperiodPipe();
    expect(pipe).toBeTruthy();
  });
});
